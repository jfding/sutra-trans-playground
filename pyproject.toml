[project]
name = "metaso-cli"
version = "0.1.0"
description = "CLI tool for calling LLM APIs with streaming support"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "flask-cors>=6.0.2",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
