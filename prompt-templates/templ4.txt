请严格遵循以下规则对文言文文本进行切块处理：
核心目标与长度：以 150字 为核心目标长度进行切分。每个文本块的实际字数允许在 130字至180字 之间灵活浮动。语义完整性为绝对优先原则，字数要求次之。
语义边界切分：切分必须严格发生在语义完整处，例如：
一个完整的问答回合结束。
一个独立的论点或论述阐述完毕。
一个叙事场景或事件描述完结。
严禁在对话中途或逻辑紧密的句群内部切断。
重叠区域规则（固定20字）：为保持上下文连贯，在相邻文本块之间设置约20个字的重叠区。具体操作如下：
在确定一个文本块的结束边界后，将该块结束位置之前的约20个字（需保持其相对完整），复制到下一个文本块的起始部分。
这20字应尽可能是一个完整的短句或句群片段，避免出现断头句。
输出格式：按顺序输出所有文本块，每个块为独立段落。重叠部分会自然呈现在相邻块的衔接处。
【待处理文言文文本如下:】
【{input_txt}】
